spring:
  application:
    name: db-syncer-e2e-tests

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

  flyway:
    enabled: true

logging:
  level:
    com.dbsyncer: INFO
    org.springframework: INFO
